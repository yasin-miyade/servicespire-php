module.exports = {
    mode: 'jit', // Ensure JIT mode is enabled
    purge: ['./index.html', './src/**/*.{js,ts,jsx,tsx,php}'], // Ensure correct paths
    theme: {
      extend: {},
    },
    plugins: [],
  };
  